<div class = "container mt-5">
  <h1 class = "mx-auto">【会員一覧】</h1>
  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th>名前</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.name, user_path(user) %></td>
        </tr>
      <% end %>
    </tbody>