<% if it_word.bookmarked_by?(current_user) %>
  <td><%= link_to "♥", it_word_bookmark_path(it_word, it_word.bookmarks), method: :delete, remote: true %></td>
<% else %>
  <td><%= link_to "♡", it_word_bookmarks_path(it_word), method: :post, remote: true %></td>
<% end %>